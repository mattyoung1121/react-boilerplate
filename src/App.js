import React from "react";
import "./App.scss";

const App = () => (
  <div className="app">
    <header className="app-header">
      <h1>New React App</h1>
    </header>
  </div>
);

export default App;
